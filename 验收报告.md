# T16 验收报告

- 验收日期：2026-06-06
- 验收分支：`task/t16-acceptance`
- 验收基线：`203b3ee merge(task/T08-ui-shell): 并入 main`
- 验收范围：`任务拆分.md` T16 清单、PRD 全文硬约束、PRD §7 输出结构、推荐默认方案、发布包规范。

## 总结论

T16 未通过。

当前可验证仓库状态只落到 T08；`origin/main`、`main`、`task/t15-integration`、`task/t17-packaging` 等本地分支均指向同一提交 `203b3ee`。T16 的前置 `T15 + T17` 未能在当前仓库状态中被验证为已合并，且生产 Agent、死模板库、A4 组合、胶片条渲染、发布包规范仍为占位或缺失。

## 验收依据

- `git fetch --all --prune`：远端刷新后无新提交。
- `git branch -vv --all`：`origin/main`、`main`、`task/t15-integration`、`task/t17-packaging` 均为 `203b3ee`。
- `git log --oneline --decorate --all -n 80`：可见合并记录到 T08；未见 T09-T17 实现提交。
- `node tools/_t07-test.mjs`：T07 json-rescue 现有测试 `22 通过 / 0 失败`。
- `node --input-type=module -e "...vocab..."`：词表数量为 `6 10 11 8 8`，符合 PRD §10 数量要求；Node 提示 `package.json` 未声明 `"type": "module"`。
- `test -f .aixignore`：失败，`.aixignore` 缺失。
- `rg` 静态检查：生产路径 `pages lib app.js app.json package.json AGENTS.md` 未命中 `tools/bootstrap`、`import.*figure.js`、`readFile`、`fs.`，但该结果主要来自生产模块未实现，不能替代发布包验收通过。

## 前置条件核对

- [ ] T15 已合并：未通过。当前仓库未见 T15 联调与降级演练实现或记录；`pages/index/index.ink` 仍明确写着 T14 前 mock。
- [ ] T17 已合并：未通过。`.aixignore` 缺失，发布包检查清单/脚本缺失，包根仍包含 `tools/bootstrap/`、README 与多份开发文档。

## PRD 7 条硬约束

| # | 硬约束 | 结果 | 依据 |
|---|---|---|---|
| 1 | 意图在前，A1 顺序追问，context 足够后才并行 | [ ] 未通过 | `lib/agents/intent.js` 待实现；`runAgent()` 仍是 T08 mock，未接 A1、追问循环、A2/A3 并行屏障。 |
| 2 | 死模板为静态资产，生产只调用、不生成不改写；A2 只输出模板编号 | [ ] 未通过 | `lib/storyboard-library.js`、`lib/library.js`、`lib/agents/composition.js` 均待实现；无可调用死模板资产。 |
| 3 | 模板 4 字段齐：图片构图、分镜说明、决策指引、场景适配 | [ ] 未通过 | 死模板库为空占位，无 `templates[]`、无 `figure`、无 `caption/decisionHint/sceneFit` 可验。 |
| 4 | 两层色仅 `#000` + `#40FF5E`，Canvas 写死 hex | [ ] 未通过 | UI CSS 有黑底/绿的 fallback，但 `lib/renderer/filmstrip.js` 待实现，Canvas 未绘制，无法证明示意图只用两色。 |
| 5 | 每方案 3-6 镜 | [ ] 未通过 | A4 未实现；mock markdown 仅写“占位（3~6）”，没有真实 board 长度约束。 |
| 6 | 运镜手法收敛在 2-3 种以内、情绪基调单一 | [ ] 未通过 | A2/A4 护栏未实现；无 `distinct(camera)<=3` 与单 mood 校验。 |
| 7 | 全走 AIUI 官方 API + 设计规范 | [ ] 部分通过但整体未通过 | 已有 `SpeechRecognition`、`streamdown`、`canvas` 节点和 480/黑底/卡片/12px/2px 样式；缺 `LanguageModel` A1/A2/A3 会话、A2/A3 双会话并行、`wx.createCanvasContext` 渲染器。 |

## PRD §7 输出结构

| 输出要求 | 结果 | 依据 |
|---|---|---|
| 标题 + 情绪基调 + 所用模板编号 + 分镜数组 | [ ] 未通过 | 当前 markdown 是占位文案，未由 A4 board 确定性生成。 |
| (a) 分镜数量 = 数组长度，顶部明示 3-6 | [ ] 未通过 | 无真实数组；仅有占位“分镜数量：占位（3~6）”。 |
| (b) 每镜时长 | [ ] 未通过 | A3 与 A4 未实现，无每镜 duration。 |
| (c) 串联剪辑数组 | [ ] 未通过 | `buildEditingFlow()` 未实现。 |
| (d) 一句话画面 + 镜头调度语言 + 景别 + 构图 | [ ] 未通过 | A2/A4 未实现，无真实每镜说明。 |
| 每镜两层色剪辑分镜示意图 | [ ] 未通过 | Canvas 节点存在，但渲染器未实现、不绘制。 |
| 逐镜分镜指引 | [ ] 未通过 | UI 中为“指引占位”，`buildGuidance()` 未实现。 |

## 推荐默认方案

- [ ] A4 纯 JS 无 LLM：未通过。`lib/agents/combine.js` 是待实现占位；虽然没有 LLM 调用，但也没有 A4 组合逻辑。
- [ ] 死模板 18-24 条，每条含情绪标签 + 场景：未通过。`lib/storyboard-library.js` 为空占位。
- [ ] 分镜指引无 LLM 润色，决策/可执行导向：未通过。`buildGuidance()` 未实现，UI 只展示占位指引。

## T16 清单逐项勾选

- [ ] 意图在前、A1 就绪后才并行：未通过。
- [ ] 生产流程无模板生成；A2 只调用死模板编号：未通过。
- [ ] 死模板 4 字段齐、figure 可绘制、每条含情绪标签 + 场景：未通过。
- [ ] 示意图仅 `#000` + `#40FF5E`，hex 写死：未通过。
- [ ] 每方案 3-6 镜：未通过。
- [ ] 运镜手法 <=2-3 种、情绪单一：未通过。
- [ ] 全走官方 API（SpeechRecognition/LanguageModel 双会话/streamdown/canvas）：未通过。
- [x] 480/黑底/卡片/12px/2px/无 emoji：通过静态核对。`pages/index/index.ink` 已有 480px、黑底、卡片、12px 圆角、2px 边框；页面实现未发现 emoji。
- [ ] 输出含 §7 (a)镜数 (b)时长 (c)串联剪辑数组 (d)简要说明 + 逐镜示意图 + 指引：未通过。
- [ ] A4 全程无 LLM；分镜指引无润色：未通过。
- [ ] 发布包规范通过（无离线工具泄漏 / 无运行时读文件依赖）：未通过。

## 发布包规范核对

- [ ] 必含项完整：未通过。必含文件存在，但 `lib/storyboard-library.js`、`lib/library.js`、`lib/agents/*`、`lib/renderer/filmstrip.js` 多数仍为占位，不能视为完整生产包。
- [ ] 必排除项已配置：未通过。`.aixignore` 缺失。
- [ ] 包内容扫描：未通过。当前包根包含 `tools/bootstrap/`、README、PRD、架构与任务文档；未形成发布包过滤结果。
- [x] 权限检查：通过静态核对。`AGENTS.md` 仅列 `microphone`、`network`。
- [x] 路由检查：通过静态核对。`app.json` 指向 `pages/index/index`，对应页面存在。
- [ ] 无离线工具泄漏：未通过。仓库根仍包含 `tools/bootstrap/`，且没有 `.aixignore` 证明上传时排除。
- [ ] 无运行时读文件/JSON 依赖：无法完整通过。静态 grep 未发现生产路径读文件依赖，但 `loadLibrary()` 尚未实现，不能证明运行时只静态 import 死模板库。

## 遗留缺陷

1. 前置不满足：当前可验证基线没有 T15/T17 合并内容，T16 依赖链断开。
2. T09/T10/T11 生产 Agent 全部待实现，无法验证 A1 顺序追问、A2/A3 并行、A2 死模板调用、A3 节奏输出。
3. T04/T05/T06 死模板库链路缺失，`storyboard-library.js`、`library.js`、A0 校验脚本均为占位。
4. T12 A4 缺失，无法验证 3-6 镜、camera 护栏、单 mood、Markdown、§7 board、guidance、editingFlow。
5. T13 胶片条渲染缺失，无法验证两层色 Canvas、`wx.createCanvasContext`、N=3/N=6 实测、canvas null 降级。
6. T14 编排接线缺失，UI 仍为 T08 mock，不接真实 A1-A4。
7. T15 联调降级记录缺失，无法验证 LM/SpeechRecognition/A2/A3/json/canvas 故障降级。
8. T17 发布包规范缺失，`.aixignore` 与上传前检查脚本/清单未落地，离线工具未从发布包规则中排除。
9. `package.json` 未声明 `"type": "module"`，Node 运行 ESM 文件时出现 `MODULE_TYPELESS_PACKAGE_JSON` 警告；AIUI 运行时是否受影响需单独确认。

## 已验证通过的辅助项

- T07 json-rescue 现有脏样本测试通过：`22 通过 / 0 失败`。
- T02 词表数量符合 PRD §10：景别 6、镜头调度 10、构图 11、转场 8、情绪 8。
- UI 骨架具备 SpeechRecognition 生命周期、streamdown 节点、canvas 节点、error-state、硬件键入口与基础视觉规范。
