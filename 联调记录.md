# T15 联调与降级演练记录

- 日期：2026-06-06
- worktree：`/Users/smalldog/Desktop/sd-worktrees/t15-integration`
- 分支：`task/t15-integration`
- 基线：`203b3ee merge(task/T08-ui-shell): 并入 main`
- 规格来源：`任务拆分.md` 的 T15、`架构设计.md` §6.2

## 当前实现状态

当前仓库尚未达到 T15 的完整联调前置条件。页面已落地 T08 UI 状态机与语音管线 mock，但真实 Agent 编排与渲染链路仍未实现：

- `pages/index/index.ink`：`runAgent()` 仍是 T08 mock，占位展示固定 3 条 `filmstripCells` 和 3 条 `guidance`。
- `lib/agents/intent.js`、`composition.js`、`rhythm.js`、`combine.js`：均为 1 行“待实现”占位。
- `lib/renderer/filmstrip.js`：1 行“待实现”占位。
- `lib/library.js`、`lib/storyboard-library.js`：1 行“待实现”占位。
- `tools/bootstrap/a0-validate.js`：1 行占位，执行后退出 0 但无实际校验输出。

因此本次 T15 记录把结果分为：

- **已实测**：当前代码可直接执行或可用页面对象模拟验证。
- **静态核验**：通过源码检索确认当前实现状态。
- **阻塞**：因 T09/T10/T12/T13/T14 等前置实现缺失，无法执行真实故障注入。

## 端到端走查

使用 Node 临时提取 `pages/index/index.ink` 的 `<script setup>` 并实例化页面对象，不落盘测试脚本。

结果：

```json
{
  "speechMissing": {
    "phase": "error",
    "errorText": "当前运行时不支持语音输入"
  },
  "mockE2E": {
    "phase": "result",
    "markdownHasQuery": true,
    "guidanceCount": 3,
    "filmstripCells": 3
  }
}
```

结论：

- T08 mock 端到端可从 `runAgent(query)` 进入 `result`，并显示用户 query。
- 真实 A1→[A2/A3]→A4→canvas 端到端链路未接入，T15 完整验收阻塞。

## 故障注入矩阵

| 故障项 | 执行/核验方式 | 观察结果 | §6.2 期望降级 | 结论 |
|---|---|---|---|---|
| `SpeechRecognition` 缺失 | 临时页面实例中令 `SpeechRecognition === undefined` 后调用 `onLoad()` | `phase=error`，`errorText=当前运行时不支持语音输入` | 进 `error`，提示运行时不支持语音 | **符合** |
| `LanguageModel.availability() !== 'available'` | `rg` 静态核验 `LanguageModel` | 仅注释中出现，运行时代码未引入 `LanguageModel` 或 availability 检测 | A1 启动前检测，进 `error`「AI 运行时暂不可用」，不进入并行 | **阻塞：T14/A1 未实现** |
| A2 单路失败 | `wc -l lib/agents/composition.js` 与 `rg Promise.allSettled` | `composition.js` 为 1 行占位；`Promise.allSettled` 仅在 TODO 注释中 | A2 失败走 `nearestTemplate()` 死模板最近邻兜底，A3 成功照常组合 | **阻塞：T09/T14 未实现** |
| A3 单路失败 | `wc -l lib/agents/rhythm.js` 与 `rg Promise.allSettled` | `rhythm.js` 为 1 行占位；`Promise.allSettled` 仅在 TODO 注释中 | A3 失败走模块B确定性表，A2 成功照常组合 | **阻塞：T10/T14 未实现** |
| JSON 脏返回 | `node tools/_t07-test.mjs` | 22 通过 / 0 失败，覆盖围栏、散文、尾逗号、单引号、全角引号、截断等样本 | `json-rescue.js` 解析失败返回 `null`，由各 Agent 做领域回退 | **解析层符合；Agent 领域回退阻塞** |
| 词表越界 id | `node --input-type=module -e` 调用 `pick(MOODS, 'out_of_vocab', 'calm')` 和 `pick(TRANSITIONS, 'unknown', 'cut')` | 输出回退到 `calm/平静治愈` 与 `cut/硬切` | 字段级清洗：id 不在词表取 vocab 默认值 | **工具层符合；Agent 字段清洗阻塞** |
| `wx.createCanvasContext` 返回 null | `rg createCanvasContext` 与 `wc -l lib/renderer/filmstrip.js` | `createCanvasContext` 仅在 TODO 注释中；`filmstrip.js` 为 1 行占位 | 重试 5 次仍 null 后隐藏 canvas，Markdown 文字版兜底，不阻断结果展示 | **阻塞：T13 未实现** |
| N=3 胶片条尺寸实测 | 当前 mock `runAgent()` 与 CSS 静态核验 | mock 固定 `filmstripCells.length=3`；CSS 固定 `width: 894px; height: 180px`；无真实绘制 | N=3 左对齐绘制，固定最大 backing 或按 key 重建，无错位残影 | **阻塞：只能核验占位数据与 CSS，不能实测 canvas** |
| N=6 胶片条尺寸实测 | 用含“6个镜头”的 query 调用当前 mock `runAgent()` | 仍返回 `filmstripCells.length=3`，没有按 shotCount 生成 N=6 | N=6 对齐成图、scroll-x 可见全镜、无残影 | **不符合/阻塞：T14/T13 未实现** |

## 验证命令与结果

```bash
git -C /Users/smalldog/Desktop/分镜导演Agent worktree add /Users/smalldog/Desktop/sd-worktrees/t15-integration -b task/t15-integration
```

结果：退出 0，创建 `task/t15-integration`，HEAD 为 `203b3ee`。

```bash
node tools/_t07-test.mjs
```

结果：22 通过 / 0 失败。Node 同时提示 `MODULE_TYPELESS_PACKAGE_JSON`，建议 `package.json` 增加 `"type": "module"`；本次未改配置，因为 T15 要求不改业务逻辑。

```bash
find app.js lib tools -name '*.js' -print -exec node --check {} \;
```

结果：退出 0，覆盖 `app.js`、`lib/` 与 `tools/` 下所有 `.js` 文件的语法检查。

```bash
node tools/bootstrap/a0-validate.js
```

结果：退出 0，无输出。原因：该文件当前仍是占位注释，不代表 A0 库校验真实通过。

```bash
node --input-type=module -e "import { pick, MOODS, TRANSITIONS } from './lib/vocab.js'; console.log(JSON.stringify({mood: pick(MOODS, 'out_of_vocab', 'calm'), transition: pick(TRANSITIONS, 'unknown', 'cut')}));"
```

结果：

```json
{"mood":{"id":"calm","label":"平静治愈"},"transition":{"id":"cut","label":"硬切","desc":"直接切换，节奏利落"}}
```

```bash
rg -n "typeof SpeechRecognition|LanguageModel|allSettled|createCanvasContext|width: 894px|height: 180px|filmstripCells" pages/index/index.ink lib
```

结果：确认语音缺失检测已在 `onLoad()`/`startListening()` 中存在；`LanguageModel`、`Promise.allSettled`、`createCanvasContext` 仅出现在 TODO 注释；胶片条 CSS 固定为 `894px × 180px`。

```bash
wc -l lib/agents/intent.js lib/agents/composition.js lib/agents/rhythm.js lib/agents/combine.js lib/renderer/filmstrip.js lib/library.js lib/storyboard-library.js tools/bootstrap/a0-validate.js
```

结果：上述文件均为 1 行占位。

## 小修记录

无。本次只新增联调记录，不改业务逻辑。

## 风险与后续前置

T15 完整通过前必须先补齐：

- T05/T06：死模板库与 `nearestTemplate()`。
- T09/T10：A2/A3 真实运行与领域回退。
- T12：A4 组合、字段清洗、N=3..6 对齐。
- T13：canvas 渲染器、null 重试 5 次、文字版降级。
- T14：真实 A1→`Promise.allSettled([A2,A3])`→A4→渲染接线，以及 `LanguageModel.availability()` 检测。
